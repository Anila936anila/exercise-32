"use strict";
// Checking usernames: 
//Make a list of five or morenusernames called current user.
// Step 1.
const current_users = ["Anila", "Mahreen", "Sobia", "Rubab", "Asifa"];
const new_users = ["Anil", "Mahr", "Sobi", "Rub", "AsIf"];
";
{
    const lowercasedCurrentUsers = current_users.map(user => user.toLowerCase());
    for (; ; )
        ;
}
